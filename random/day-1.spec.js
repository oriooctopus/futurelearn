var assert = require('assert');

// write a function that reverses a string
// abc -> cba
// racecar -> racebar
// rtyui -> iuytr
const reverseString = () => {};

const validateReverseString = () => {
  reverseString('abc');
};

// const runTests = () => {};

describe('validate reverse string', () => {
  it('should work', () => {
    expect(2).to.be(2);
  });
});
